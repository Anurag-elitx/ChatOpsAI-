apiVersion: v1
kind: Service
metadata:
  name: chatops-api-service
  labels:
    app: chatops-api
    tier: backend
spec:
  selector:
    app: chatops-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
